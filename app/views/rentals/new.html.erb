<%= form_with(model: @rental, local: true) do |f| %>
  <p><%= f.label :start_date, "Data de inicio" %></p>
  <p><%= f.date_field :start_date %></p>
  <p><%= f.label :end_date, "Data de término" %></p>
  <p><%= f.date_field :end_date %></p>
  <p><%= f.label :client_id, "Cliente" %></p>
  <p><%= f.collection_select(:client_id, @clients, :id, :description) %></p>
  <p><%= f.label :car_category_id, "Categoria do carro" %></p>
  <p><%= f.collection_select(:car_category_id, @car_categories, :id, :name) %></p>
  <p><%= f.submit 'Enviar' %></p>
<% end %>